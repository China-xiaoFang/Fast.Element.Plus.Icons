# Fast.Element.Plus.Icons AI 编码规范

适用于本仓库全部文件。本仓库把 SVG 源文件生成 Vue 图标组件，图形、导出名称与属性透传都属于消费者可见合同。

## 仓库定位与阅读入口

- `icons/`：SVG 原始输入。
- `scripts/generate-icons.ts`：校验、命名、生成组件和导出入口。
- `src/icons/` 及由生成器维护的入口：需要与输入保持一致的生成源码。
- `package.json`、`tsdown.config.ts`、`tests/`：Vue peer、产物、运行时和类型验证。

## 工作原则

- 默认用简体中文沟通。简单问题直接给结论；复杂问题说明关键依据、取舍和未确认事项。
- 修改前按影响范围阅读源码、公开类型、配置、调用方、相关测试和文档；不要只凭文件名、历史聊天或示例推断合同。
- 先确认当前分支与工作区修改。保留已有未提交内容；相同文件存在重叠修改时先厘清归属，不回退他人的工作。
- 语言和框架的正式语义优先于历史写法。发现旧实现有误应说明证据并修正相关问题，但不能借“最佳实践”扩大任务。
- 以完成需求所需的合理改动为目标。可以重构有问题的内部实现，不进行无关全仓格式化、目录迁移或依赖升级。
- 跨仓库访问按本文件的明确授权及目标仓库适用规则执行；授权范围内的正常协作不重复询问。未列入范围的仓库和操作不因同属 Fast 生态而自动获得授权；实际沙箱、权限及任务中的明确限制仍需遵守。
- 新增文件放入现有职责目录。只在需求确有需要时引入模块、依赖或抽象；不自动创建总结、计划、架构报告等文件。
- 使用与任务匹配的可用 Skill，但以实际工具链和项目合同为准；不因 Skill 的偏好改换技术栈或自动安装、更新 Skill。

## SDK 合同与发布边界

- 将包名、公开导出路径、类型推断、默认值、异常方式、运行行为及兼容范围视为公开合同，不能只检查函数名称是否变化。
- 新增 API 优先复用既有概念与命名。新增可选能力不得无意改变现有调用；必要的不兼容调整先说明影响与迁移办法，再按明确授权实施。
- `package.json` 的 `exports`、类型入口、依赖分类、`sideEffects`、发布文件和构建配置共同决定包合同；不能只改源码而遗漏产物。
- 只维护实际声明支持的模块格式和平台，不凭历史印象新增或删除 CJS、IIFE、ESM 等产物，也不把浏览器打包器可用等同于 Node 可直接运行。
- Node、pnpm、TypeScript 与依赖版本从当前配置和锁文件读取。安装依赖使用本仓库包管理方式，不额外生成其他包管理器锁文件。
- 不擅自提升运行时、peer 范围或主依赖版本；出现兼容冲突时分析真实原因，不用强制安装或关闭检查掩盖。
- 版本号、发布范围和发布行为不是普通重构的附带动作。需要版本调整时给出依据，按明确的版本或发布任务执行。

## TypeScript 与代码表达

- 局部变量和内部函数优先使用清晰的类型推断；公开边界提供足够的类型信息，避免推断结果意外泄露内部类型。
- 优先表达真实数据结构和约束。不使用 `any`、双重断言、非空断言或 suppression 掩盖错误；对外部输入在边界处校验并缩小类型。
- 必须适配第三方缺陷时，使用最小范围、可解释的兼容处理；不要通过全局放宽类型或 lint 规则绕过问题。
- 保持自然的 `async/await`、事件回调和生命周期写法；区分同步抛错与 Promise 拒绝，不为了形式增加 `async`、`void` 或包装函数。
- 不吞掉异常，不把失败转成伪成功；资源、监听器、定时器和临时状态应在适当边界释放。
- 命名、导入顺序、换行与缩进服从仓库 ESLint、Prettier、TypeScript 配置。不手工维护与格式化工具竞争的排版规则。

## 本仓库的编码约定

- 新增或调整图标先修改 SVG 源或生成逻辑，再运行生成器；不手工修补生成组件而不更新其来源。
- 保留文件名到公共 PascalCase 组件名的稳定映射；大小写调整、名称冲突和别名可能影响消费者，不能只当文件清理。
- `viewBox`、比例、fill、stroke、分组和多色图形都是视觉合同。不能为了统一风格把所有颜色改成 `currentColor` 或自动压平结构。
- 输入校验需要考虑脚本、事件属性、外部资源、危险 URL 与非法标记。不能把简单文本替换等同于可靠的 SVG 安全解析。
- 生成应确定、排序稳定、支持只读漂移检查；只更新生成器拥有的明确文件，不能用危险清理命令删除整个源码目录。
- 保持 Vue 根 SVG 的 attrs、class、style、尺寸、role 与 `aria-*` 透传。不要把具有正常语义的可访问性属性过滤掉。
- 遵循 Vue TSX 的 `class`、属性、事件和渲染语义，不套用 React 类型与运行时约定。
- 运行时不应依赖 Node；导入包不应执行图标生成或读取磁盘。保留按实际导出可验证的 Tree Shaking 与模块合同。
- 同一组生成数据适合做一致性检查，但还需独立验证代表性图标渲染、属性透传和公开导出，不能只比较两个同步生成的列表。
- 图标数量变化不要求更新固定数量断言；删除或重命名公开图标仍需独立的兼容性审阅。

## 注释与文档

- TypeScript 公共 API 使用 TSDoc；JavaScript 在需要类型或合同说明时使用 JSDoc。内部代码只注释必要的原因、约束和边界。
- 说明参数语义、默认行为、返回、异常和兼容性中无法由签名直接看出的部分；不要给每个成员机械堆齐所有标签。
- 标签型短说明通常不使用结束标点；完整陈述、原因和限制正常使用标点。不批量给所有注释加句号或删除句号。
- 保留有价值注释并修正过时说明。保持已有多语言文档的职责，不混入无关翻译。
- 公共合同变化时，更新对应注释、实际维护的文档和示例；先确认文档来源与生成流程，不另建重复手册。

## 测试质量与验证

- 测试公开合同和可观察行为，不测试无意义的源码拼写、内部文件拆分或固定 API 数量。
- 新增行为增加相应测试；修复缺陷优先保留可复现的回归用例。内部重构未改变合同，不应随实现一起改写预期答案。
- 类型测试验证合法调用与应被拒绝的调用；运行时测试验证真实结果；包测试验证构建产物和消费者入口，三者不能相互替代。
- 只 mock 外部边界。隔离时间、网络、环境、全局状态和临时资源；不依赖真实账号、真实业务服务或不可控公网。
- 复用现有运行器和测试发现方式。没有对应能力时不要编造命令或创建空测试；是否建设测试基础设施由任务与风险决定。
- 不通过删断言、跳过失败用例、放宽规则、批量接受快照或排除源码让检查变绿。覆盖率用于发现风险，不代替断言质量。
- 先做与改动相关的检查，再按影响范围运行完整检查。核对脚本调用链，避免重复构建和 `check`、`prepack`、包测试递归。
- 交付时说明修改、实际执行的命令及结果、未执行项与剩余风险。只读审查、编译成功和实际运行验证分别表述。

## 常用验证入口

```text
pnpm run generate:check
pnpm run typecheck
pnpm run test:types
pnpm run test:runtime
pnpm run test:package
pnpm run lint
pnpm run format:check
pnpm run build
pnpm run check
```

修改生成输入后使用 `pnpm run generate`，审查源文件和生成结果的对应差异；日常检查优先使用只读的 `generate:check`。

以下命令与职责以当前 `package.json` 为准；脚本调整后同步维护本节。`test`、`check` 可能包含构建或其他检查，先查看组合关系再执行。

## Fast.Docs 协作与版本同步

- 对有修改目的的开发任务，已授权读取同级 `Fast.Docs`，并更新与本仓库实际公开包对应的文档、示例、变更说明和版本核对记录；这是正常交付的一部分，不因跨仓库再次询问。只读审查、仅建议任务或用户明确排除文档时，不自动写入，报告待同步项。
- 相对路径以本仓库 Git 根目录为基准，默认文档位置为 `../Fast.Docs`，不是相对终端当前子目录。先核实仓库身份、实际路径、分支和未提交修改；worktree 或非同级布局使用已明确配置的映射，不猜测路径、不扫描无关目录。
- 写入前主动读取 Fast.Docs 根及目标目录适用的 AGENTS 规则，并定位现有产品导航、包名映射与生成流程。允许对当前包的入口、示例依赖和校验配置作必要的最小联动，不授权重做站点结构、批量升级依赖或修改其他产品文档。
- 本任务实际更新包版本，或处理已明确纳入本任务且尚未同步的版本变更时，必须核对变化并同步对应文档。公开 API、默认值、类型、行为、兼容范围或使用方式变化，即使尚未升版本，也同步相关说明；不因为该规则自行决定升版。
- 根据实际差异和可确定的上次核对版本检查指南、API、示例、迁移说明及版本要求。没有文档正文变化时，不制造无意义改写；完成必要核对与验证后再更新已有的文档审阅记录，按实际包分别维护，不能只对齐字符串。
- 目标版本尚未发布时，不把 Fast.Docs 的运行依赖盲目改成无法安装的版本。使用已授权的可追溯本地产物或现有预发布验证方式；源码只读核对不隐含在其他仓库构建或写入的授权，不能静默回退旧包冒充新版本验证。
- 执行与改动相关的现有文档构建、链接、API/示例及版本检查；不得删除检查、自动接受公共 API 基线变化或把未验证状态写成已通过。最终分别报告代码版本、文档同步和验证结果。
- Fast.Docs 缺失、映射不明、存在重叠未提交修改或环境/沙箱阻止访问时，说明具体阻塞和待同步内容，继续其他可安全完成的工作；不擅自创建替代文档仓库、覆写内容、放宽权限或绕过审批。此授权不包括提交、推送、发布及部署。

## ESLint 本地复制规范

- 本仓库 ESLint 策略以同级 `Fast.ESLint.Config` 为唯一规则基准。已授权只读该源仓库中与同步相关的规则、配置、注释、插件依赖和测试；不因核对基准再次询问，不在同步任务中反向修改源仓库。
- 继续使用目标仓库自己的本地配置文件；同步不改为安装/依赖 `@fast-china/eslint-config`，不从相邻仓库源码动态导入，保证目标仓库独立可用。必要的第三方 ESLint 插件正常保留。
- 保留适用规则的名称、等级、选项、注释文本、相对顺序及配置的文件范围和覆盖语义。不用“最佳实践”改写规则，不删减或改写注释，不在目标仓库增加私有规则或后置覆盖来绕过基准。
- 最终消费配置中的静态 `rules`、`files`、`ignores` 和配置片段直接在使用位置铺开；不提取为 `const`/`let`/`var`、数组变量、规则映射或工厂函数，不用 `...baseRules`、`...tsRules` 等隐藏实际规则。不改用其他声明方式规避该要求。
- 允许必要的模块 import、官方配置包装器、解析器/插件对象及必须按运行环境求值的表达式；不把函数、正则或模块对象错误序列化，也不把运行时路径提前固定为作者机器路径。此内联约定仅作用于复制后的消费配置，不限制 SDK 源码或生成器的正常抽象。
- 不适用的 React、Angular 等配置按完整能力模块排除，同时清理确实仅服务于该模块的无用引用；不能只删除其中几条不喜欢的规则，也不能把源 SDK 对外提供的可选能力删除。
- 文件匹配、tsconfig 路径、忽略目录等必要工程适配只改变项目边界，不改变规则政策，并逐项说明。配置冲突先分析，不能为了 lint 通过擅自放宽基准或顺带全仓重构。
- 同步后验证实际 ESLint 加载、代表性文件的有效配置、未定义规则及适用的 lint/typecheck；保持语义一致与逐项注释保留，不能仅比较文件长度或固定规则数量。兼容问题及未通过项如实报告。

## Git 提交说明

- 完成已授权的仓库或包版本更新后，在最终答复中输出可直接复制的推荐 Git 提交说明；有实际文档改动的 Fast.Docs 另给一份，即使文档站自身版本未变。仅提出版本建议、没有实际版本调整时，不得描述成已经更新。
- 根据本任务实际差异、版本变化及 `git status`、暂存/未暂存差异生成说明；区分既有修改，不将不属于本任务的内容混入提交建议。不要为生成说明执行暂存、取消暂存或提交操作。
- 默认使用 `type(scope): 中文摘要`；`feat`、`fix`、`refactor`、`docs`、`chore` 按实际主要改动选择。单纯版本调整使用 `chore(release)`；版本更新伴随功能或修复时，不要只写“更新版本”而省略实质变化。
- 每个独立仓库分别给出一个纯文本代码块，标题为首行；必要的正文与标题之间空一行。仓库名与解释放在代码块外，方便复制到 Git 图形客户端。
- 只描述真实完成的改动。未发布不写“已发布”，未运行验证不写“测试通过”；阻塞或未完成事项在代码块外单独说明。没有有效变更的仓库不生成空提交建议。
- 推荐提交说明不代表执行提交。未经另行明确授权，不执行 `git add`、`commit`、`push`、`tag` 或发布；用户要求命令时再输出供其复制的命令，并按真实差异限定文件范围。

## 安全边界

- 不输出、保存或提交密码、Token、Secret、API Key、私钥及带凭据的地址。示例使用明确占位符；诊断输出须脱敏。
- 不直接执行数据库写入、结构修改、自动迁移或数据初始化；需要时仅生成 SQL 或方案供人工审核，测试环境也不自动建库或写库。
- 未经明确授权，不执行 `git commit`、`push`、`reset --hard`、`clean`、`rebase` 或历史重写，不发布 npm/NuGet 包，不修改远程服务。
- 不使用危险批量删除命令，不覆盖未提交修改。执行构建、测试、安装或工程脚本前，检查其自定义目标和生命周期是否包含发布、危险清理或外部写入。
- 共享 AI 规则与个人会话、缓存、凭据分开管理。不要为了隐藏正式代码或文档变更扩大 `.gitignore`；不擅自修改全局工具配置、Hook、MCP 或权限。
